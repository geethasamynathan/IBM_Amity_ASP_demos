@model IEnumerable<EFDBFirstDemousingMVC.Models.Employee>
@{
    ViewBag.Title = "GetAllEmployee";
}

<h2>Employee List</h2>
<ul>
    @foreach (var emp in Model)
    {
        <li>@Html.ActionLink(emp.Name,"Details",new { id = emp.Empid })</li>
    }
</ul>

